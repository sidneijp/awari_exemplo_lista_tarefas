const outra_funcao_qualquer = function(texto, variavel) {
    if (variavel) {
        variavel *= 10
    }
    return `${texto}: ${variavel}`
}

class MinhaClass {
    a
    constructor(){
        this.a = 123
    }
}
